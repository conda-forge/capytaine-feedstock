{% set version = "1.1" %}
{% set hash = "0ca0c010bbfe4887b2b4678a2d9e6a069f7726a5e61f831f266283fc557578f4" %}

package:
  name: capytaine
  version: {{ version }}

source:
  url: https://github.com/mancellin/capytaine/archive/v{{version}}.tar.gz
  sha256: {{ hash }}

build:
  skip: True  # [py<36]
  number: 0
  script: "{{ PYTHON }} -m pip install . --no-deps -vv"
  entry_points:
    - capytaine = capytaine.ui.cli:main

requirements:
  build:
    - {{ compiler('fortran') }}
    - {{ compiler('c') }}
    - {{ compiler('m2w64_c') }}  # [win]
    - {{ compiler('m2w64_fortran') }}  # [win]
    - llvm-openmp  # [osx]
  host:
    - python
    - pip
    - setuptools
    - numpy
    - llvm-openmp  # [osx]
    - m2w64-toolchain  # [win]
    - libpython  # [win]
  run:
    - python
    - attrs
    - {{ pin_compatible('numpy') }}
    - pandas
    - matplotlib
    - scipy
    - vtk
    - xarray
    - llvm-openmp  # [osx]

test:
  imports:
    - capytaine
  requires:
    - pytest
    - hypothesis
  source_files:
    - pytest/*.py
    - pytest/**/*
  commands:
    - pytest
    - capytaine --help

about:
  home: https://github.com/mancellin/capytaine
  license: GPL-3.0
  license_family: GPL
  license_file: LICENSE
  summary: A Python-based linear potential flow solver based on Nemoh

extra:
  recipe-maintainers:
    - mancellin
